// import { createFeatureSelector, createSelector } from "@ngrx/store";
// import { State } from "./app.state";
// import { RegionState } from "../reducers/region.reducer";
// import { CountryState } from "../reducers/countries.reducer";

// const getState = createFeatureSelector<State>('app');

// export const getRegions = createSelector(
//   getState,
//   (state: RegionState) => state.regions
// );

// export const getSelectedRegion = createSelector(
//   getState,
//   (state: RegionState) => state.selectedRegion
// );

// export const getCountries = createSelector(
//   getState,
//   (state: CountryState) => state.countries
// );

// export const getSelectedCountry = createSelector(
//   getState,
//   (state: CountryState) => state.selectedCountry
// );

// export const getError = createSelector(
//   getState,
//   (state: State) => state.error
// );
